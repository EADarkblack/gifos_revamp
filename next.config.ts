import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "media.giphy.com",
      "picsum.photos",
      "media4.giphy.com",
      "media3.giphy.com",
      "media1.giphy.com",
      "media2.giphy.com",
      "media0.giphy.com",
    ],
  },
};

export default nextConfig;
